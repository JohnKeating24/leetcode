// 主要分为三步：1 找到中点，分割 2 逆转后半断链表 3 合并前、后(逆转后)半断链表  
// 每步都写得很巧妙
// 1  直接令 ListNode *p1 = head, *p2 = head->next;找到的p1是中点的前一点；正好分割。
//    若令，ListNode *p1 = head, *p2 = head;找到的中点恰好是中点，无法分割。
// 2  直接用while循环 逆转后半断链表，不用子函数
// 3  迭代过程非常巧妙。  
