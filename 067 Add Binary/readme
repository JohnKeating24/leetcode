// 本质就是将两个字符串逆序逐位相加，再将结果逆序输出。
// 注意：1 字符'0'、'1'和数字0、1的转换，只有数字0、1才能实现期望的相加；字符'0'的ASCII码是48。  2 短字符串的处理。

//            char ai=i<a.size() ? a[i] : '0';
//            char val='0'+(ai-'0'+bi-'0'+carry)%2;
//或者
//            int ai=i<a.size() ? a[i]-'0' : 0;
//            int val=(ai+bi+carry)%2;
