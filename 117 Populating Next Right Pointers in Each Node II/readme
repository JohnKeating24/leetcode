/*这题如果直接用层次遍历的框架来做，会产生log(n)的空间；但是也能够通过测试*/

//写得非常好：用头节点的指针实现了常数空间
//解答的本质是实现层次遍历：
//for循环处理每一层的节点：即连接其 有效子节点 ，(右空指针是默认的)
//下一层已经连接完毕，故要转到处理下一层，只需要找到其头节点，即dummy.next
//转到处理下一层的递归用 connect(dummy.next) 实现
