// 分三种情况：
// 1 p[j]=='*',记录下star=j++,match=i    //此时j++是因为默认'*'匹配0个字符。
// 2 s[i]==p[j] || p[j]=='?' 此时同时,i++,j++
// 3 star>0表示:p中有'*'，此时重置i,j,实现'*'能匹配0-n个字符的功能：具体为：i = ++match;j = star + 1;
// 4 不符合这三种情况，return false

// 若s中元素均符合前三种情况，且p中的剩余元素均为'*'，则匹配成功。


//注意：当有*号时，是匹配0-n个字符。实现这个功能的是
//i = ++match;
//j = star + 1;
//其中i = ++match可拆分为 match=match+1;i=match;其含义为：依次从match后的0、1、2、3、、、个字符与star+1处的字符匹配。
